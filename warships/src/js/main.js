// code goes brrr

const ROUTINGTARGET = document.querySelector("#root");

const NavLinks = document.querySelectorAll("a[data-href]");

const RoutingTable = {
    "/" : {page: "start.html", code: null},
    "/country" : {page: "country.html", code: null}
}

const LoadJSON = () => {
    
}

const NavClickEvent = () => {

}
